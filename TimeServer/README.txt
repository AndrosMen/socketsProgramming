Usage

	Run the Server script and pass a valid PortNumber (0 to 65535) as an argument
		example: 
			Server 3001
	
	Run the Client script specifing the host address and the port as arguments
		example: 
			Client localhost 3001

